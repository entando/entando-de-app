<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <groupId>org.entando.entando</groupId>
    <artifactId>entando-de-app</artifactId>
    <packaging>war</packaging>
    <version>7.0.0-SNAPSHOT</version>
    <name>entando-de-app</name>
    <url>http://www.entando.com/</url>
    <licenses>
        <license>
            <name>GNU LESSER GENERAL PUBLIC LICENSE, Version 3, 29 June 2007</name>
            <url>https://www.gnu.org/licenses/lgpl-3.0.txt</url>
            <distribution>repo</distribution>
        </license>
    </licenses>
    <organization>
        <name>Entando Inc.</name>
        <url>http://www.entando.com/</url>
    </organization>
    <repositories>
        <repository>
            <id>jx-releases</id>
            <url>https://nexus-jx.apps.serv.run/repository/maven-releases/</url>
            <releases>
                <enabled>true</enabled>
            </releases>
            <snapshots>
                <enabled>false</enabled>
            </snapshots>
        </repository>
    </repositories>
    <properties>
        <!-- COMPONENTS -->
        <entando-engine.version>7.0.0-ENG-3006-PR-213</entando-engine.version>
        <entando-admin-console.version>7.0.0-ENG-3006-PR-103</entando-admin-console.version>
        <entando-portal-ui.version>7.0.0-ENG-3006-PR-36</entando-portal-ui.version>
        <entando-plugin-jacms.version>7.0.0-ENG-3006-PR-133</entando-plugin-jacms.version>
        <entando-plugin-jpmail.version>7.0.0-ENG-3006-PR-33</entando-plugin-jpmail.version>
        <entando-plugin-jpversioning.version>7.0.0-ENG-3006-PR-33</entando-plugin-jpversioning.version>
        <entando-plugin-jpseo.version>7.0.0-ENG-3006-PR-57</entando-plugin-jpseo.version>
        <entando-plugin-jpcontentscheduler.version>7.0.0-ENG-3006-PR-35</entando-plugin-jpcontentscheduler.version>
        <entando-plugin-jpredis.version>7.0.0-ENG-3006-PR-18</entando-plugin-jpredis.version>
        <entando.keycloak.plugin.version>7.0.0-ENG-3006-PR-56</entando.keycloak.plugin.version>

        <!-- SONAR -->
        <github.organization>entando</github.organization>
        <sonar.host.url>https://sonarcloud.io</sonar.host.url>
        <sonar.organization>${github.organization}</sonar.organization>
        <sonar.projectKey>${github.organization}_${project.artifactId}</sonar.projectKey>

        <!-- TESTS -->
        <test.database.driver>org.apache.derby.jdbc.EmbeddedDriver</test.database.driver>
        <test.database.hostname>localhost</test.database.hostname>
        <test.database.port>1527</test.database.port>
        <test.database.username>agile</test.database.username>
        <test.database.password>agile</test.database.password>
        <test.database.port.url>
            jdbc:derby:${project.build.directory}/test/db/${project.artifactId}testPort;create=true
        </test.database.port.url>
        <test.database.serv.url>
            jdbc:derby:${project.build.directory}/test/db/${project.artifactId}testServ;create=true
        </test.database.serv.url>
        <docker.host.address>172.17.0.1</docker.host.address>
        <env.db.environment>production</env.db.environment>
        <entando.engine.web.context>${project.build.finalName}</entando.engine.web.context>
        <skipDocker>true</skipDocker>
        <skipSmokeTests>true</skipSmokeTests>
        <autoCreateNetwork>true</autoCreateNetwork>
        <entando.version>6.3.0-SNAPSHOT</entando.version>
        <postgres.driver.version>9.4.1212</postgres.driver.version>
        <jetty.version>10.0.7</jetty.version>

        <!-- Keycloak Configuration -->
        <keycloak.enabled>false</keycloak.enabled>
        <keycloak.auth.url>http://localhost:8081/auth</keycloak.auth.url>
        <keycloak.realm>entando</keycloak.realm>
        <keycloak.client.id>entando-app</keycloak.client.id>
        <keycloak.client.secret>b4b34472-9926-4753-9db8-a50f152df3da</keycloak.client.secret>
        <keycloak.public.client.id>entando-web</keycloak.public.client.id>

        <!-- CORS Configuration -->
        <cors.access.control.allow.origin>*</cors.access.control.allow.origin>
        <cors.access.control.allow.headers>Content-Type, Authorization</cors.access.control.allow.headers>
        <cors.access.control.allow.methods>GET, POST, PUT, DELETE, OPTIONS, PATCH</cors.access.control.allow.methods>
        <cors.access.control.allow.credentials>false</cors.access.control.allow.credentials>
        <cors.access.control.maxAge>3600</cors.access.control.maxAge>
        <log4j.version>2.16.0</log4j.version>
    </properties>
    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.entando</groupId>
                <artifactId>entando-core-parent</artifactId>
                <version>6.4.6</version>
                <scope>import</scope>
                <type>pom</type>
            </dependency>
            <!--engine-->
            <dependency>
                <groupId>org.entando.entando</groupId>
                <artifactId>entando-engine</artifactId>
                <version>${entando-engine.version}</version>
            </dependency>
            <dependency>
                <groupId>org.entando.entando</groupId>
                <artifactId>entando-engine</artifactId>
                <version>${entando-engine.version}</version>
                <type>test-jar</type>
                <scope>test</scope>
            </dependency>
            <!--admin-console-->
            <dependency>
                <groupId>org.entando.entando</groupId>
                <artifactId>entando-admin-console</artifactId>
                <version>${entando-admin-console.version}</version>
                <type>war</type>
            </dependency>
            <dependency>
                <groupId>org.entando.entando</groupId>
                <artifactId>entando-admin-console</artifactId>
                <version>${entando-admin-console.version}</version>
                <classifier>classes</classifier>
            </dependency>
            <dependency>
                <groupId>org.entando.entando</groupId>
                <artifactId>entando-admin-console</artifactId>
                <version>${entando-admin-console.version}</version>
                <type>test-jar</type>
                <scope>test</scope>
            </dependency>
            <!--portal-ui-->
            <dependency>
                <groupId>org.entando.entando</groupId>
                <artifactId>entando-portal-ui</artifactId>
                <version>${entando-portal-ui.version}</version>
                <classifier>classes</classifier>
            </dependency>
            <dependency>
                <groupId>org.entando.entando</groupId>
                <artifactId>entando-portal-ui</artifactId>
                <version>${entando-portal-ui.version}</version>
                <type>war</type>
            </dependency>
            <dependency>
                <groupId>org.entando.entando</groupId>
                <artifactId>entando-portal-ui</artifactId>
                <version>${entando-portal-ui.version}</version>
                <type>test-jar</type>
                <scope>test</scope>
            </dependency>
            <!--jacms-->
            <dependency>
                <groupId>org.entando.entando.plugins</groupId>
                <artifactId>entando-plugin-jacms</artifactId>
                <version>${entando-plugin-jacms.version}</version>
                <classifier>classes</classifier>
                <type>jar</type>
            </dependency>
            <dependency>
                <groupId>org.entando.entando.plugins</groupId>
                <artifactId>entando-plugin-jacms</artifactId>
                <version>${entando-plugin-jacms.version}</version>
                <type>war</type>
            </dependency>
            <!--jpmail-->
            <dependency>
                <groupId>org.entando.entando.plugins</groupId>
                <artifactId>entando-plugin-jpmail</artifactId>
                <version>${entando-plugin-jpmail.version}</version>
                <classifier>classes</classifier>
            </dependency>
            <dependency>
                <groupId>org.entando.entando.plugins</groupId>
                <artifactId>entando-plugin-jpmail</artifactId>
                <version>${entando-plugin-jpmail.version}</version>
                <type>war</type>
            </dependency>
            <!--jpversioning-->
            <dependency>
                <groupId>org.entando.entando.plugins</groupId>
                <artifactId>entando-plugin-jpversioning</artifactId>
                <version>${entando-plugin-jpversioning.version}</version>
                <classifier>classes</classifier>
            </dependency>
            <dependency>
                <groupId>org.entando.entando.plugins</groupId>
                <artifactId>entando-plugin-jpversioning</artifactId>
                <version>${entando-plugin-jpversioning.version}</version>
                <type>war</type>
            </dependency>
            <!--jpseo-->
            <dependency>
                <groupId>org.entando.entando.plugins</groupId>
                <artifactId>entando-plugin-jpseo</artifactId>
                <version>${entando-plugin-jpseo.version}</version>
                <classifier>classes</classifier>
                <exclusions>
                    <exclusion>
                        <groupId>org.entando.entando.plugins</groupId>
                        <artifactId>entando-plugin-jacms</artifactId>
                    </exclusion>
                </exclusions>
            </dependency>
            <dependency>
                <groupId>org.entando.entando.plugins</groupId>
                <artifactId>entando-plugin-jpseo</artifactId>
                <version>${entando-plugin-jpseo.version}</version>
                <type>war</type>
            </dependency>
            <dependency>
                <groupId>org.entando.entando.plugins</groupId>
                <artifactId>entando-plugin-jpredis</artifactId>
                <version>${entando-plugin-jpredis.version}</version>
                <classifier>classes</classifier>
            </dependency>
            <dependency>
                <groupId>org.entando.entando.plugins</groupId>
                <artifactId>entando-plugin-jpredis</artifactId>
                <version>${entando-plugin-jpredis.version}</version>
                <type>war</type>
            </dependency>
            <!--keycloak-->
            <dependency>
                <groupId>org.entando.entando</groupId>
                <artifactId>entando-keycloak-auth</artifactId>
                <version>${entando.keycloak.plugin.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>
    <dependencies>
        <!--TODO clean this up. we are controlling these in the dependencyManagement seciotn -->
        <dependency>
            <groupId>org.entando.entando</groupId>
            <artifactId>entando-engine</artifactId>
        </dependency>
        <dependency>
            <groupId>org.entando.entando</groupId>
            <artifactId>entando-admin-console</artifactId>
            <classifier>classes</classifier>
        </dependency>
        <dependency>
            <groupId>org.entando.entando</groupId>
            <artifactId>entando-portal-ui</artifactId>
            <classifier>classes</classifier>
        </dependency>
        <dependency>
            <groupId>org.entando.entando.plugins</groupId>
            <artifactId>entando-plugin-jacms</artifactId>
            <classifier>classes</classifier>
        </dependency>
        <dependency>
            <groupId>org.entando.entando.plugins</groupId>
            <artifactId>entando-plugin-jpmail</artifactId>
            <classifier>classes</classifier>
        </dependency>
        <dependency>
            <groupId>org.entando.entando.plugins</groupId>
            <artifactId>entando-plugin-jpversioning</artifactId>
            <classifier>classes</classifier>
        </dependency>
        <dependency>
            <groupId>org.entando.entando.plugins</groupId>
            <artifactId>entando-plugin-jpseo</artifactId>
            <classifier>classes</classifier>
        </dependency>
        <dependency>
            <groupId>org.entando.entando.plugins</groupId>
            <artifactId>entando-plugin-jpredis</artifactId>
            <classifier>classes</classifier>
        </dependency>
        <dependency>
            <groupId>org.entando.entando</groupId>
            <artifactId>entando-keycloak-auth</artifactId>
        </dependency>
        <dependency>
            <groupId>javax.servlet.jsp</groupId>
            <artifactId>jsp-api</artifactId>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>javax.servlet</groupId>
            <artifactId>javax.servlet-api</artifactId>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.apache.logging.log4j</groupId>
            <artifactId>log4j-web</artifactId>
            <version>${log4j.version}</version>
        </dependency>
        <!--test dependencies -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-api</artifactId>
        </dependency>
        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-junit-jupiter</artifactId>
        </dependency>

            <!-- war overlays -->
            <dependency>
                <groupId>org.entando.entando</groupId>
                <artifactId>entando-admin-console</artifactId>
                <type>war</type>
            </dependency>
            <dependency>
                <groupId>org.entando.entando</groupId>
                <artifactId>entando-portal-ui</artifactId>
                <type>war</type>
            </dependency>
            <dependency>
                <groupId>org.entando.entando.plugins</groupId>
                <artifactId>entando-plugin-jacms</artifactId>
                <type>war</type>
            </dependency>
            <dependency>
                <groupId>org.entando.entando.plugins</groupId>
                <artifactId>entando-plugin-jpmail</artifactId>
                <type>war</type>
            </dependency>
            <dependency>
                <groupId>org.entando.entando.plugins</groupId>
                <artifactId>entando-plugin-jpseo</artifactId>
                <type>war</type>
            </dependency>
            <dependency>
                <groupId>org.entando.entando.plugins</groupId>
                <artifactId>entando-plugin-jpredis</artifactId>
                <type>war</type>
            </dependency>
            <dependency>
                <groupId>org.entando.entando.plugins</groupId>
                <artifactId>entando-plugin-jpversioning</artifactId>
                <type>war</type>
            </dependency>
        <!-- test dependencies -->
        <dependency>
            <groupId>org.entando.entando</groupId>
            <artifactId>entando-engine</artifactId>
            <type>test-jar</type>
        </dependency>
        <dependency>
            <groupId>org.entando.entando</groupId>
            <artifactId>entando-admin-console</artifactId>
            <type>test-jar</type>
        </dependency>
        <dependency>
            <groupId>org.entando.entando</groupId>
            <artifactId>entando-portal-ui</artifactId>
            <type>test-jar</type>
        </dependency>
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <version>${postgres.driver.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.apache.derby</groupId>
            <artifactId>derby</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.apache.derby</groupId>
            <artifactId>derbyclient</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.apache.commons</groupId>
            <artifactId>commons-dbcp2</artifactId>
            <scope>test</scope>
        </dependency>

    </dependencies>

    <profiles>
        <profile>
            <id>jetty-local</id>
            <properties>
                <!--Image configuration -->
                <skipDocker>false</skipDocker>
                <skipAppBuilderImage>false</skipAppBuilderImage>
                <jee.server>jetty</jee.server>
                <entando.engine.address>localhost</entando.engine.address>
                <app.builder.depends.on></app.builder.depends.on>

                <skipServerImage>true</skipServerImage>
                <jboss.home.in.image>none</jboss.home.in.image>
                <server.base.image>none</server.base.image>

                <derby.base.folder>${project.build.directory}/derby/${env.db.environment}</derby.base.folder>
                <!--Hard coded ports for local development -->
                <appbuilder.port>5000</appbuilder.port>
                <entando.engine.port>8080</entando.engine.port>
                <db.server.local.port>${db.server.port}</db.server.local.port>

                <cors.access.control.allow.origin>http://localhost:3000</cors.access.control.allow.origin>
                <cors.access.control.allow.credentials>true</cors.access.control.allow.credentials>
            </properties>
        </profile>
        <profile>
            <id>local-ports</id>
            <properties>
                <!--Hard coded ports for local development -->
                <appbuilder.port>5000</appbuilder.port>
                <db.server.local.port>${db.server.port}</db.server.local.port>
                <entando.engine.port>8080</entando.engine.port>
            </properties>
        </profile>
        <profile>
            <id>eap</id>
            <properties>
                <!--Image configuration -->
                <skipDocker>false</skipDocker>
                <skipAppBuilderImage>false</skipAppBuilderImage>

                <jee.server>eap</jee.server>
                <entando.engine.address>${docker.host.address}</entando.engine.address>
                <app.builder.depends.on>${project.artifactId}-engine</app.builder.depends.on>

                <skipServerImage>false</skipServerImage>
                <jboss.home.in.image>/opt/eap</jboss.home.in.image>
                <server.base.image>entando-eap71-clustered-base</server.base.image>

                <derby.base.folder>/entando-data/databases</derby.base.folder>
            </properties>
        </profile>
        <profile>
            <id>wildfly</id>
            <properties>
                <!--Image configuration -->
                <skipDocker>false</skipDocker>
                <skipAppBuilderImage>false</skipAppBuilderImage>
                <jee.server>wildfly</jee.server>

                <entando.engine.address>${docker.host.address}</entando.engine.address>
                <app.builder.depends.on>${project.artifactId}-engine</app.builder.depends.on>

                <skipServerImage>false</skipServerImage>
                <jboss.home.in.image>/wildfly</jboss.home.in.image>
                <server.base.image>entando-wildfly12-base</server.base.image>

                <derby.base.folder>/entando-data/databases</derby.base.folder>
            </properties>
        </profile>
        <profile>
            <id>jetty-container</id>
            <properties>
                <!--Image configuration -->
                <skipDocker>false</skipDocker>
                <jee.server>jetty</jee.server>

                <skipAppBuilderImage>false</skipAppBuilderImage>

                <entando.engine.address>${docker.host.address}</entando.engine.address>
                <app.builder.depends.on>${project.artifactId}-engine</app.builder.depends.on>

                <skipServerImage>false</skipServerImage>
                <jboss.home.in.image>/wildfly</jboss.home.in.image>
                <server.base.image>entando-jetty948-base</server.base.image>

                <derby.base.folder>/entando-data/databases</derby.base.folder>
            </properties>
        </profile>
        <profile>
            <id>derby</id>
            <properties>
                <db.vendor>derby</db.vendor>
                <skipDatabaseImage>true</skipDatabaseImage>
                <entando.engine.depends.on></entando.engine.depends.on>
                <!--DB connection properties-->
                <portdb.database>entandoPort</portdb.database>
                <portdb.url>jdbc:derby:${derby.base.folder}/${portdb.database};create=true</portdb.url>
                <portdb.username>agile</portdb.username>
                <portdb.password>agile</portdb.password>
                <portdb.driverClassName>org.apache.derby.jdbc.EmbeddedDriver</portdb.driverClassName>
                <portdb.jndi>java:/jdbc/portDataSource</portdb.jndi>

                <servdb.database>entandoServ</servdb.database>
                <servdb.url>jdbc:derby:${derby.base.folder}/${servdb.database};create=true</servdb.url>
                <servdb.username>agile</servdb.username>
                <servdb.password>agile</servdb.password>
                <servdb.driverClassName>org.apache.derby.jdbc.EmbeddedDriver</servdb.driverClassName>
                <servdb.jndi>java:/jdbc/servDataSource</servdb.jndi>

                <!-- DB work required from the server image. Create the template Derby databases -->
                <server.init.command>$ENTANDO_COMMON_PATH/init-derby-from-war.sh --war-file=${jboss.home.in.image}/standalone/deployments/${project.build.finalName}.war</server.init.command>
            </properties>
        </profile>
        <profile>
            <id>postgresql</id>
            <properties>
                <db.vendor>postgresql</db.vendor>
                <skipDatabaseImage>false</skipDatabaseImage>
                <entando.engine.depends.on>${project.artifactId}-dbms</entando.engine.depends.on>
                <db.base.image>entando-postgresql95-base</db.base.image>
                <db.init.command>$STI_SCRIPTS_PATH/init-postgresql-from-war.sh --war-file=/tmp/${project.build.finalName}.war</db.init.command>
                <db.volume.mount.path>/var/lib/pgsql/data</db.volume.mount.path>
                <db.server.port>5432</db.server.port>

                <portdb.database>portdb</portdb.database>
                <portdb.url>jdbc:postgresql://${docker.host.address}:${db.server.local.port}/${portdb.database}
                </portdb.url>
                <portdb.username>portdbuser</portdb.username>
                <portdb.password>portdb123</portdb.password>
                <portdb.jndi>java:/jdbc/portDataSource</portdb.jndi>
                <portdb.driverClassName>org.postgresql.Driver</portdb.driverClassName>

                <servdb.database>servdb</servdb.database>
                <servdb.url>jdbc:postgresql://${docker.host.address}:${db.server.local.port}/${servdb.database}
                </servdb.url>
                <servdb.username>servdbuser</servdb.username>
                <servdb.password>servdb123</servdb.password>
                <servdb.jndi>java:/jdbc/servDataSource</servdb.jndi>
                <servdb.driverClassName>org.postgresql.Driver</servdb.driverClassName>

                <!-- No DB work required from the server image. Just set the build_id -->
                <server.init.command>echo $(date +%s) > /entando-data-templates/build_id</server.init.command>
            </properties>
        </profile>
        <profile>
            <id>mysql</id>
            <properties>
                <db.vendor>mysql</db.vendor>
                <skipDatabaseImage>false</skipDatabaseImage>
                <entando.engine.depends.on>${project.artifactId}-dbms</entando.engine.depends.on>
                <db.base.image>entando-mysql57-base</db.base.image>
                <db.init.command>$STI_SCRIPTS_PATH/init-mysql-from-war.sh --war-file=/tmp/${project.build.finalName}.war</db.init.command>
                <db.volume.mount.path>/var/lib/mysql/data</db.volume.mount.path>
                <db.server.port>3306</db.server.port>

                <portdb.database>portdb</portdb.database>
                <portdb.url>jdbc:mysql://${docker.host.address}:${db.server.local.port}/${portdb.database}</portdb.url>
                <portdb.username>portdbuser</portdb.username>
                <portdb.password>portdb123</portdb.password>
                <portdb.jndi>java:/jdbc/portDataSource</portdb.jndi>
                <portdb.driverClassName>com.mysql.jdbc.Driver</portdb.driverClassName>

                <servdb.database>servdb</servdb.database>
                <servdb.url>jdbc:mysql://${docker.host.address}:${db.server.local.port}/${servdb.database}</servdb.url>
                <servdb.username>servdbuser</servdb.username>
                <servdb.password>servdb123</servdb.password>
                <servdb.jndi>java:/jdbc/servDataSource</servdb.jndi>
                <servdb.driverClassName>com.mysql.jdbc.Driver</servdb.driverClassName>

                <!-- No DB work required from the server image. Just set the build_id -->
                <server.init.command>echo $(date +%s) > /entando-data-templates/build_id</server.init.command>
            </properties>
        </profile>
        <profile>
            <id>keycloak</id>
            <properties>
                <keycloak.enabled>true</keycloak.enabled>
            </properties>
        </profile>
    </profiles>
    <build>
        <finalName>${project.artifactId}</finalName>
        <resources>
            <resource>
                <directory>src/main/resources</directory>
            </resource>
            <resource>
                <directory>src/main/conf</directory>
                <filtering>true</filtering>
                <targetPath>WEB-INF/conf</targetPath>
            </resource>
            <resource>
                <directory>src/main/java</directory>
                <includes>
                    <include>**/*.properties</include>
                    <include>**/*.xml</include>
                    <include>**/*.xsd</include>
                </includes>
            </resource>
            <resource>
                <directory>src/main/tld</directory>
                <targetPath>META-INF</targetPath>
                <includes>
                    <include>**/*.tld</include>
                </includes>
            </resource>
        </resources>
        <testResources>
            <testResource>
                <directory>src/test/config/conf</directory>
                <targetPath>${project.build.directory}/test/conf</targetPath>
                <filtering>true</filtering>
            </testResource>
            <testResource>
                <directory>src/test/resources</directory>
                <targetPath>${project.build.directory}/test-classes</targetPath>
                <filtering>true</filtering>
            </testResource>
        </testResources>
        <plugins>
            <!-- Fabric8 docker plugin -->
            <plugin>
                <groupId>io.fabric8</groupId>
                <artifactId>docker-maven-plugin</artifactId>
                <version>0.26.1</version>
                <executions>
                    <execution>
                        <id>build-docker-image</id>
                        <phase>package</phase>
                        <goals>
                            <goal>build</goal>
                        </goals>
                    </execution>
                </executions>
                <configuration>
                    <autoCreateCustomNetworks>${autoCreateNetwork}</autoCreateCustomNetworks>
                    <skip>${skipDocker}</skip>
                    <startParallel>true</startParallel>
                    <portPropertyFile>${project.build.directory}/ports.properties</portPropertyFile>
                    <verbose>true</verbose>
                    <imagePullPolicy>IfNotPresent</imagePullPolicy>
                    <volumes>
                        <volume>
                            <!--Volume for JBoss and Derby data-->
                            <name>${project.artifactId}-entando-data</name>
                            <driver>local</driver>
                        </volume>
                        <volume>
                            <!--Volume for PostgreSQL/MySQL data-->
                            <name>${project.artifactId}-entando-db-data</name>
                            <driver>local</driver>
                        </volume>
                    </volumes>
                    <images>
                        <image>
                            <name>entando/${project.artifactId}-${db.vendor}:${project.version}</name>
                            <alias>${project.artifactId}-dbms</alias>
                            <build>
                                <skip>${skipDatabaseImage}</skip>
                                <from>entando/${db.base.image}:6.1.0</from>
                                <assembly>
                                    <descriptorRef>artifact</descriptorRef>
                                    <targetDir>/tmp</targetDir>
                                </assembly>
                                <env>
                                    <PORTDB_USERNAME>${portdb.username}</PORTDB_USERNAME>
                                    <PORTDB_PASSWORD>${portdb.password}</PORTDB_PASSWORD>
                                    <PORTDB_DATABASE>${portdb.database}</PORTDB_DATABASE>
                                    <PORTDB_JNDI>${portdb.jndi}</PORTDB_JNDI>
                                    <SERVDB_USERNAME>${servdb.username}</SERVDB_USERNAME>
                                    <SERVDB_PASSWORD>${servdb.password}</SERVDB_PASSWORD>
                                    <SERVDB_DATABASE>${servdb.database}</SERVDB_DATABASE>
                                    <SERVDB_JNDI>${servdb.jndi}</SERVDB_JNDI>
                                    <DB_ENVIRONMENT>${env.db.environment}</DB_ENVIRONMENT>
                                    <ALGO_DEFAULT_KEY>changeit</ALGO_DEFAULT_KEY>
                                </env>
                                <runCmds>
                                    <run>${db.init.command}</run>
                                </runCmds>
                            </build>
                            <run>
                                <skip>${skipDatabaseImage}</skip>
                                <namingStrategy>alias</namingStrategy>
                                <network>
                                    <mode>custom</mode>
                                    <name>${project.artifactId}-network</name>
                                    <alias>${project.artifactId}-dbms</alias>
                                </network>
                                <ports>
                                    <port>+docker.host.address:db.server.local.port:${db.server.port}</port>
                                </ports>
                                <volumes>
                                    <bind>
                                        <volume>${project.artifactId}-entando-db-data:${db.volume.mount.path}</volume>
                                    </bind>
                                </volumes>
                                <wait>
                                    <tcp>
                                        <ports>
                                            <port>${db.server.port}</port>
                                        </ports>
                                    </tcp>
                                    <time>120000</time>
                                </wait>

                                <log>
                                    <enabled>true</enabled>
                                    <prefix>dbms:</prefix>
                                    <color>cyan</color>
                                </log>
                            </run>
                        </image>
                        <image>
                            <name>entando/${project.artifactId}-${jee.server}:${project.version}</name>
                            <alias>${project.artifactId}-engine</alias>
                            <build>
                                <from>entando/${server.base.image}:6.3.4</from>
                                <skip>${skipServerImage}</skip>
                                <assembly>
                                    <descriptorRef>artifact</descriptorRef>
                                    <targetDir>${jboss.home.in.image}/standalone/deployments</targetDir>
                                </assembly>
                                <nocache>true</nocache>
                                <env>
                                    <!--These will be overridden at runtime, but we need it in case Derby needs to be initialized-->
                                    <PORTDB_USERNAME>${portdb.username}</PORTDB_USERNAME>
                                    <PORTDB_PASSWORD>${portdb.password}</PORTDB_PASSWORD>
                                    <PORTDB_URL>jdbc:derby:${derby.base.folder}/${portdb.database};create=true
                                    </PORTDB_URL>
                                    <PORTDB_JNDI>${portdb.jndi}</PORTDB_JNDI>
                                    <SERVDB_USERNAME>${servdb.username}</SERVDB_USERNAME>
                                    <SERVDB_PASSWORD>${servdb.password}</SERVDB_PASSWORD>
                                    <SERVDB_URL>jdbc:derby:${derby.base.folder}/${servdb.database};create=true
                                    </SERVDB_URL>
                                    <SERVDB_JNDI>${servdb.jndi}</SERVDB_JNDI>
                                    <DB_ENVIRONMENT>${env.db.environment}</DB_ENVIRONMENT>
                                    <DB_STARTUP_CHECK>true</DB_STARTUP_CHECK>
                                    <LOGNAME>/var/log/entando/engine.log
                                    </LOGNAME><!--workaround until upstream images are fixed-->
                                    <ALGO_DEFAULT_KEY>changeit</ALGO_DEFAULT_KEY>
                                    <KEYCLOAK_ENABLED>${keycloak.enabled}</KEYCLOAK_ENABLED>
                                    <KEYCLOAK_REALM>${keycloak.realm}</KEYCLOAK_REALM>
                                    <KEYCLOAK_AUTH_URL>${keycloak.auth.url}</KEYCLOAK_AUTH_URL>
                                    <KEYCLOAK_CLIENT_ID>${keycloak.client.id}</KEYCLOAK_CLIENT_ID>
                                    <KEYCLOAK_CLIENT_SECRET>${keycloak.client.secret}</KEYCLOAK_CLIENT_SECRET>
                                    <KEYCLOAK_PUBLIC_CLIENT_ID>${keycloak.public.client.id}</KEYCLOAK_PUBLIC_CLIENT_ID>
                                    <CORS_ENABLED>${cors.enabled}</CORS_ENABLED>
                                    <CORS_ACCESS_CONTROL_ALLOW_ORIGIN>${cors.access.control.allow.origin}
                                    </CORS_ACCESS_CONTROL_ALLOW_ORIGIN>
                                    <CORS_ACCESS_CONTROL_ALLOW_HEADERS>${cors.access.control.allow.headers}
                                    </CORS_ACCESS_CONTROL_ALLOW_HEADERS>
                                    <CORS_ACCESS_CONTROL_ALLOW_METHODS>${cors.access.control.allow.methods}
                                    </CORS_ACCESS_CONTROL_ALLOW_METHODS>
                                    <CORS_ACCESS_CONTROL_ALLOW_CREDENTIALS>${cors.access.control.allow.credentials}
                                    </CORS_ACCESS_CONTROL_ALLOW_CREDENTIALS>
                                    <CORS_ACCESS_CONTROL_MAX_AGE>${cors.access.control.maxAge}
                                    </CORS_ACCESS_CONTROL_MAX_AGE>
                                </env>
                                <runCmds>
                                    <run>${server.init.command}</run>
                                </runCmds>
                            </build>
                            <run>
                                <skip>${skipServerImage}</skip>
                                <namingStrategy>alias</namingStrategy>
                                <network>
                                    <mode>custom</mode>
                                    <name>${project.artifactId}-network</name>
                                    <alias>${project.artifactId}-engine</alias>
                                </network>
                                <volumes>
                                    <bind>
                                        <volume>${project.artifactId}-entando-data:/entando-data</volume>
                                    </bind>
                                </volumes>
                                <env>
                                    <PORTDB_USERNAME>${portdb.username}</PORTDB_USERNAME>
                                    <PORTDB_PASSWORD>${portdb.password}</PORTDB_PASSWORD>
                                    <PORTDB_URL>${portdb.url}</PORTDB_URL>
                                    <PORTDB_JNDI>${portdb.jndi}</PORTDB_JNDI>
                                    <SERVDB_USERNAME>${servdb.username}</SERVDB_USERNAME>
                                    <SERVDB_PASSWORD>${servdb.password}</SERVDB_PASSWORD>
                                    <SERVDB_URL>${servdb.url}</SERVDB_URL>
                                    <SERVDB_JNDI>${servdb.jndi}</SERVDB_JNDI>
                                    <DB_ENVIRONMENT>${env.db.environment}</DB_ENVIRONMENT>
                                    <DB_STARTUP_CHECK>false</DB_STARTUP_CHECK>
                                    <KEYCLOAK_ENABLED>${keycloak.enabled}</KEYCLOAK_ENABLED>
                                    <KEYCLOAK_REALM>${keycloak.realm}</KEYCLOAK_REALM>
                                    <KEYCLOAK_AUTH_URL>${keycloak.auth.url}</KEYCLOAK_AUTH_URL>
                                    <KEYCLOAK_CLIENT_ID>${keycloak.client.id}</KEYCLOAK_CLIENT_ID>
                                    <KEYCLOAK_CLIENT_SECRET>${keycloak.client.secret}</KEYCLOAK_CLIENT_SECRET>
                                    <KEYCLOAK_PUBLIC_CLIENT_ID>${keycloak.public.client.id}</KEYCLOAK_PUBLIC_CLIENT_ID>
                                    <CORS_ENABLED>${cors.enabled}</CORS_ENABLED>
                                    <CORS_ACCESS_CONTROL_ALLOW_ORIGIN>${cors.access.control.allow.origin}
                                    </CORS_ACCESS_CONTROL_ALLOW_ORIGIN>
                                    <CORS_ACCESS_CONTROL_ALLOW_HEADERS>${cors.access.control.allow.headers}
                                    </CORS_ACCESS_CONTROL_ALLOW_HEADERS>
                                    <CORS_ACCESS_CONTROL_ALLOW_METHODS>${cors.access.control.allow.methods}
                                    </CORS_ACCESS_CONTROL_ALLOW_METHODS>
                                    <CORS_ACCESS_CONTROL_ALLOW_CREDENTIALS>${cors.access.control.allow.credentials}
                                    </CORS_ACCESS_CONTROL_ALLOW_CREDENTIALS>
                                    <CORS_ACCESS_CONTROL_MAX_AGE>${cors.access.control.maxAge}
                                    </CORS_ACCESS_CONTROL_MAX_AGE>
                                </env>
                                <dependsOn>
                                    <container>${entando.engine.depends.on}</container>
                                </dependsOn>
                                <ports>
                                    <port>+docker.host.address:entando.engine.port:8080</port>
                                </ports>
                                <wait>
                                    <http>
                                        <url>
                                            http://${docker.host.address}:${entando.engine.port}/${project.artifactId}
                                        </url>
                                    </http>
                                    <time>120000</time>
                                </wait>
                                <log>
                                    <enabled>true</enabled>
                                    <prefix>server:</prefix>
                                    <color>blue</color>
                                </log>
                            </run>
                        </image>
                        <image>
                            <name>entando/appbuilder-with-digital-exchange:5.2.0</name>
                            <run>
                                <skip>${skipAppBuilderImage}</skip>
                                <network>
                                    <mode>custom</mode>
                                    <name>${project.artifactId}-network</name>
                                    <alias>appbuilder</alias>
                                </network>
                                <ports>
                                    <port>+docker.host.address:appbuilder.port:5000</port>
                                </ports>
                                <dependsOn>
                                    <container>${app.builder.depends.on}</container>
                                </dependsOn>
                                <env>
                                    <DOMAIN>
                                        http://${entando.engine.address}:${entando.engine.port}/${project.artifactId}
                                    </DOMAIN>
                                </env>
                                <wait>
                                    <http>
                                        <url>http://${docker.host.address}:${appbuilder.port}</url>
                                    </http>
                                    <time>9000</time>
                                </wait>
                                <log>
                                    <enabled>true</enabled>
                                    <prefix>appbuilder:</prefix>
                                    <color>green</color>
                                </log>
                            </run>
                        </image>
                        <image>
                            <name>entando/entando-smoke-tests:${entando.version}</name>
                            <run>
                                <skip>${skipSmokeTests}</skip>
                                <network>
                                    <mode>custom</mode>
                                    <name>${project.artifactId}-network</name>
                                    <alias>smoketests</alias>
                                </network>
                                <dependsOn>
                                    <container>${app.builder.depends.on}</container>
                                </dependsOn>
                                <cmd>mvn verify -Dtest=org.entando.selenium.smoketests.STAddTestUserTest</cmd>
                                <env>
                                    <ENTANDO_APPBUILDER_URL>
                                        http://appbuilder:5000
                                    </ENTANDO_APPBUILDER_URL>
                                </env>
                                <log>
                                    <enabled>true</enabled>
                                    <prefix>smoke-tests:</prefix>
                                    <color>red</color>
                                </log>
                            </run>
                        </image>
                    </images>
                </configuration>
            </plugin>
            <!-- End of Fabric8 docker plugin -->
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>0.8.2</version>
                <executions>
                    <execution>
                        <id>pre-unit-tests</id>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                        <configuration>
                            <!-- Sets the path to the file which contains the execution data. -->
                            <destFile>${project.build.directory}/coverage/jacoco/jacoco.exec</destFile>
                        </configuration>
                    </execution>
                    <!-- Ensures that the code coverage report for unit tests is created after unit tests have been run -->
                    <execution>
                        <id>post-unit-test</id>
                        <phase>test</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                        <configuration>
                            <excludes>
                                <exclude>src/main/java/org/entando/entando/attic/*.*</exclude>
                            </excludes>
                            <dataFile>${project.build.directory}/coverage/jacoco/jacoco.exec</dataFile>
                            <outputDirectory>${project.build.directory}/coverage/jacoco</outputDirectory>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-enforcer-plugin</artifactId>
                <version>1.0-beta-1</version>
                <executions>
                    <execution>
                        <id>enforce-versions</id>
                        <goals>
                            <goal>enforce</goal>
                        </goals>
                        <configuration>
                            <rules>
                                <requireMavenVersion>
                                    <version>3.0.0</version>
                                </requireMavenVersion>
                                <requireJavaVersion>
                                    <version>1.8</version>
                                </requireJavaVersion>
                            </rules>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>2.4</version>
                <configuration>
                    <target>1.8</target>
                    <source>1.8</source>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
                <version>2.8</version><!--$NO-MVN-MAN-VER$ -->
                <dependencies>
                    <dependency>
                        <groupId>org.codehaus.plexus</groupId>
                        <artifactId>plexus-archiver</artifactId>
                        <version>2.4.1</version>
                    </dependency>
                </dependencies>
                <executions>
                    <execution>
                        <id>unpack-dependencies-jsp-frontend-core-webapp</id>
                        <phase>initialize</phase>
                        <goals>
                            <goal>unpack</goal>
                        </goals>
                        <configuration>
                            <skip>${core-webapp-cant-overwrite}</skip>
                            <overWriteReleases>false</overWriteReleases>
                            <overWriteSnapshots>false</overWriteSnapshots>
                            <overWriteIfNewer>false</overWriteIfNewer>
                            <artifactItems>
                                <artifactItem>
                                    <groupId>org.entando.entando</groupId>
                                    <artifactId>entando-admin-console</artifactId>
                                    <type>war</type>
                                    <outputDirectory>src/main/webapp</outputDirectory>
                                    <overWrite>false</overWrite>
                                </artifactItem>
                            </artifactItems>
                            <includes>WEB-INF/aps/jsp/internalservlet/api/**/*.jsp</includes>
                            <excludes>WEB-INF/lib/*.*</excludes>
                        </configuration>
                    </execution>
                    <!--					<execution>
                            <id>unpack-dependencies-bootstrap</id>
                            <phase>prepare-package</phase>
                        <goals>
                                    <goal>unpack</goal>
                        </goals>
                        <configuration>
                                    <verbose>true</verbose>
                                    <overWriteReleases>false</overWriteReleases>
                                    <overWriteSnapshots>false</overWriteSnapshots>
                                    <overWriteIfNewer>false</overWriteIfNewer>
                                    <artifactItems>
                                            <artifactItem>
                                                    <groupId>org.entando.entando.bundles.misc</groupId>
                                                    <artifactId>entando-misc-bootstrap</artifactId>
                                                    <type>war</type>
                                                    <outputDirectory>${project.build.directory}/entando-misc-bootstrap</outputDirectory>
                                                    <overWrite>false</overWrite>
                                            </artifactItem>
                                    </artifactItems>
                                    <includes>resources/static/entando-misc-bootstrap/bootstrap/bootstrap/**</includes>
                        </configuration>
                    </execution>-->
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-war-plugin</artifactId>
                <version>3.2.2</version>
                <configuration>
                    <!-- This is explicitly declared in project deps.-->
                    <webResources>
                        <resource>
                            <directory>src/main/tld</directory>
                            <targetPath>WEB-INF/tld</targetPath>
                            <includes>
                                <include>**/*.tld</include>
                            </includes>
                            <filtering>false</filtering>
                        </resource>
                        <resource>
                            <directory>src/main/conf</directory>
                            <targetPath>WEB-INF/conf</targetPath>
                            <includes>
                                <include>**/*.properties</include>
                            </includes>
                            <filtering>true</filtering>
                        </resource>
                    </webResources>

                    <filters>
                        <filter>${*}</filter>
                    </filters>
                    <overlays>
                        <overlay>
                            <groupId>org.entando.entando</groupId>
                            <artifactId>entando-admin-console</artifactId>
                        </overlay>
                        <overlay>
                            <groupId>org.entando.entando</groupId>
                            <artifactId>entando-portal-ui</artifactId>
                        </overlay>
                        <overlay>
                            <groupId>org.entando.entando.plugins</groupId>
                            <artifactId>entando-plugin-jacms</artifactId>
                        </overlay>
                        <overlay>
                            <groupId>org.entando.entando.plugins</groupId>
                            <artifactId>entando-plugin-jpmail</artifactId>
                        </overlay>
                        <overlay>
                            <groupId>org.entando.entando.plugins</groupId>
                            <artifactId>entando-plugin-jpseo</artifactId>
                        </overlay>
                        <overlay>
                            <groupId>org.entando.entando.plugins</groupId>
                            <artifactId>entando-plugin-jpredis</artifactId>
                        </overlay>
                        <overlay>
                            <groupId>org.entando.entando.plugins</groupId>
                            <artifactId>entando-plugin-jpversioning</artifactId>
                        </overlay>
                    </overlays>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-antrun-plugin</artifactId>
                <version>1.7</version><!--$NO-MVN-MAN-VER$ -->
                <executions>
                    <execution>
                        <id>decide-if-core-webapp-can-overwrite</id>
                        <phase>validate</phase>
                        <configuration>
                            <exportAntProperties>true</exportAntProperties>
                            <target>
                                <taskdef resource="net/sf/antcontrib/antcontrib.properties"
                                         classpathref="maven.plugin.classpath"/>
                                <if>
                                    <available file="src/main/webapp/WEB-INF/aps/jsp/internalservlet/api" type="dir"/>
                                    <then>
                                        <property name="core-webapp-cant-overwrite" value="true"/>
                                    </then>
                                </if>
                            </target>
                        </configuration>
                        <goals>
                            <goal>run</goal>
                        </goals>
                    </execution>
                </executions>
                <dependencies>
                    <dependency>
                        <groupId>ant-contrib</groupId>
                        <artifactId>ant-contrib</artifactId>
                        <version>20020829</version>
                    </dependency>
                </dependencies>
            </plugin>
            <plugin>
                <groupId>org.zeroturnaround</groupId>
                <artifactId>jrebel-maven-plugin</artifactId>
                <version>1.1.3</version>
                <configuration>
                    <name>${project.artifactId}</name>
                    <version>${project.version}-${maven.build.timestamp}</version>
                </configuration>
                <executions>
                    <execution>
                        <id>generate-liverebel-xml</id>
                        <phase>process-resources</phase>
                        <goals>
                            <goal>generate-liverebel-xml</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.0.0-M1</version>
            </plugin>
            <!-- mvn jetty:run (with hot-redeploy) -->
            <plugin>
                <groupId>org.eclipse.jetty</groupId>
                <artifactId>jetty-maven-plugin</artifactId>
                <version>${jetty.version}</version>
                <configuration>
                    <scanIntervalSeconds>5</scanIntervalSeconds>
                    <webApp>
                        <resourceBases>
                            <resourceBase>src/main/webapp</resourceBase>
                            <resourceBase>${project.build.directory}/${project.build.finalName}</resourceBase>
                        </resourceBases>
                        <defaultsDescriptor>${basedir}/src/main/jetty/webdefault.xml</defaultsDescriptor>
                        <contextPath>/${project.artifactId}</contextPath>
                        <jettyEnvXml>${project.basedir}/src/main/jetty/jetty-env.xml</jettyEnvXml>
                    </webApp>
                    <systemProperties>
                        <org.eclipse.jetty.LEVEL>FATAL</org.eclipse.jetty.LEVEL>
                        <entando.version>${entando.version}</entando.version>
                        <db.migration.strategy>auto</db.migration.strategy>
                        <db.restore.enabled>true</db.restore.enabled>
                        <db.environment>production</db.environment>
                        <logFilePrefix>${project.build.directory}/logs/</logFilePrefix>
                        <logFileRotatePattern>
                            ${project.build.directory}/logs/${project.artifactId}_logs/${project.name}-%i.log.gz
                        </logFileRotatePattern>
                        <resourceDiskRootFolder>${project.basedir}/src/main/webapp/resources/</resourceDiskRootFolder>
                        <protectedResourceDiskRootFolder>${project.basedir}/src/main/webapp/protected/
                        </protectedResourceDiskRootFolder>
                        <indexDiskRootFolder>${project.build.directory}/indices</indexDiskRootFolder>
                        <portDataSourceClassName>${portdb.driverClassName}</portDataSourceClassName>
                        <servDataSourceClassName>${servdb.driverClassName}</servDataSourceClassName>
                        <portdb.url>${portdb.url}</portdb.url>
                        <portdb.jndi>${portdb.jndi}</portdb.jndi>
                        <portdb.username>${portdb.username}</portdb.username>
                        <portdb.password>${portdb.password}</portdb.password>>
                        <servdb.jndi>${servdb.jndi}</servdb.jndi>
                        <servdb.url>${servdb.url}</servdb.url>
                        <servdb.username>${servdb.username}</servdb.username>
                        <servdb.password>${servdb.password}</servdb.password>
                        <keycloak.enabled>${keycloak.enabled}</keycloak.enabled>
                        <keycloak.realm>${keycloak.realm}</keycloak.realm>
                        <keycloak.auth.url>${keycloak.auth.url}</keycloak.auth.url>
                        <keycloak.client.id>${keycloak.client.id}</keycloak.client.id>
                        <keycloak.client.secret>${keycloak.client.secret}</keycloak.client.secret>
                        <keycloak.public.client.id>${keycloak.public.client.id}</keycloak.public.client.id>
                        <cors.enabled>${cors.enabled}</cors.enabled>
                        <cors.access.control.allow.origin>${cors.access.control.allow.origin}
                        </cors.access.control.allow.origin>
                        <cors.access.control.allow.methods>${cors.access.control.allow.methods}
                        </cors.access.control.allow.methods>
                        <cors.access.control.allow.headers>${cors.access.control.allow.headers}
                        </cors.access.control.allow.headers>
                        <cors.access.control.allow.credentials>${cors.access.control.allow.credentials}
                        </cors.access.control.allow.credentials>
                        <cors.access.control.maxAge>${cors.access.control.maxAge}</cors.access.control.maxAge>
                    </systemProperties>
                </configuration>
                <dependencies>
                    <dependency>
                        <groupId>org.postgresql</groupId>
                        <artifactId>postgresql</artifactId>
                        <version>${postgres.driver.version}</version>
                    </dependency>
                    <dependency>
                        <groupId>mysql</groupId>
                        <artifactId>mysql-connector-java</artifactId>
                        <version>8.0.20</version>
                    </dependency>
                    <dependency>
                        <groupId>org.apache.derby</groupId>
                        <artifactId>derby</artifactId>
                        <version>10.9.1.0</version>
                    </dependency>
                    <dependency>
                        <groupId>org.apache.derby</groupId>
                        <artifactId>derbyclient</artifactId>
                        <version>10.9.1.0</version>
                    </dependency>
                    <dependency>
                        <groupId>org.apache.commons</groupId>
                        <artifactId>commons-dbcp2</artifactId>
                        <version>2.5.0</version>
                    </dependency>
                </dependencies>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-deploy-plugin</artifactId>
                <version>2.8.2</version>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-site-plugin</artifactId>
                <version>3.8.2</version>
            </plugin>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>license-maven-plugin</artifactId>
                <version>2.0.0</version>
                <executions>
                    <execution>
                        <id>download-licenses</id>
                        <goals>
                            <goal>download-licenses</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-clean-plugin</artifactId>
                    <version>2.5</version>
                    <configuration>
                        <excludeDefaultDirectories>true</excludeDefaultDirectories>
                        <followSymlinks>true</followSymlinks><!-- performance -->
                        <filesets>
                            <fileset>
                                <directory>${project.build.directory}</directory>
                                <excludes>
                                    <exclude>**/derby/production/**</exclude>
                                </excludes>
                            </fileset>
                        </filesets>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>
    <reporting>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-project-info-reports-plugin</artifactId>
                <version>3.0.0</version>
                <reportSets>
                    <reportSet>
                        <reports>
                            <report>summary</report>
                        </reports>
                    </reportSet>
                </reportSets>
            </plugin>

        </plugins>
    </reporting>
</project>
